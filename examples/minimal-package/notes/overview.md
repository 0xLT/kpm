# Overview

Use this small package to test the current `kpm` flow without private fixtures.
It links to [[prompt-patterns]] and [[./evaluation]] so doctor and compose can
exercise both bare filename links and relative links.

A consumer installs this package, then composes it into `wiki/@kpm-examples/ai-notes/`.
